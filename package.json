{
  "private": true,
  "scripts": {
    "start": "eleventy --serve --quiet",
    "build": "rm -rf dist && eleventy"
  },
  "engines": {
    "node": "16.x",
    "npm": "8.x"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2",
    "clean-css": "^5.1.3",
    "html-minifier": "^4.0.0",
    "music-metadata": "^7.8.5",
    "pretty-data": "^0.40.0"
  }
}
